package com.clefal.teams.client.gui.util;

import com.mojang.blaze3d.vertex.VertexConsumer;

public record DrawStringBufferInfo() implements IBufferInfo{

    @Override
    public void upload(VertexConsumer consumer) {

    }
}
